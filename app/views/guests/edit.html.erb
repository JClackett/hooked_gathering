<%= provide(:title, "Edit Guest" ) %>

<div class="outer-form">
	<h2>editing guest</h2>

	<%= render 'form', guest: @guest %>
	
	<div class="form-links">
		<ul>
			<li><%= link_to 'back', root_path %></li>
		</ul>
	</div>
</div>